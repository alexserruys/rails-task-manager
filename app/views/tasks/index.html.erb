<h1>My tasks</h1>

<ul> <% @tasks.each do |task| %>

  <li><%= link_to task.title, task_path(task)%> <%= task = task.completed ? "[x]" : "[ ]" %></li>

<% end %> </ul>

<p><%= link_to "Add a task", new_task_path %></p>


